const ATMHistory = () => {
    
    return (
        <label className="label huge">
            <h3> Account History </h3>
        </label>
    );
};

ReactDOM.render(<ATMHistory/>, document.getElementById("history"));